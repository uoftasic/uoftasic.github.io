# Archive

Superseded assets, kept for reference. Directories beginning with `_` that are
not configured as a collection are ignored by Jekyll, so nothing here is copied
into the built site.

- `asic-logo-transparent.png` — the previous hand-drawn logo. Replaced by the
  vector marks in `assets/brand/`, which scale to a favicon and print in one
  colour. Kept because it appears on older slide decks and social posts.
- `ieee_leaf_720.png` — unused IEEE UofT leaf.
